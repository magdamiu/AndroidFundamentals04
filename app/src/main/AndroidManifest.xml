<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.magdamiu.androidfundamentals04">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".fragments.ParentDynamicFragmentActivity"></activity>
        <activity android:name=".fragments.ParentActivity" />
        <activity android:name=".activities.OnSaveInstanceActivity" />
        <activity android:name=".activities.ThirdActivity" />
        <activity android:name=".recycler_view.EventDetailsActivity" />
        <activity android:name=".activities.SecondActivity" />
        <activity android:name=".activities.LifecycleTestActivity" />
        <activity android:name=".FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".recycler_view.EventsActivity" />
        <activity android:name=".custom_spinner.SpinnerActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>